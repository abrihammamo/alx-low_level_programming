#include <stdio.h>
#include "main.h"

/**
 * main - main function
 * a test for a postitive_or_negative
 * Return: zero
 *
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
